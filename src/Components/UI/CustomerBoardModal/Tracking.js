import React from 'react';

const Tracking = ({ nextStep, handleChange, tracking }) => {

    const Continue = e => {
        e.preventDefault();
        nextStep();
    }
    return (
        <div>
            
        </div>
    );
};

export default Tracking;

